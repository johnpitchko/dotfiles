- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/kitty:
      create: true
      path: config/kitty
    ~/.config/nvim:
      create: true
      path: config/nvim
    ~/.config/.rubocop.yml: config/rubocop/default.yml
    ~/.config/yarn:
      create: true
      path: config/yarn
    ~/.bundle/config:
      create: true
      path: bundle/config
    ~/.default-gems: default-gems
    ~/.irbrc: irbrc
    ~/.zprofile:
      force: true
      path: zprofile
    ~/.zshrc:
      force: true
      path: zshrc

- brew:
  - asdf
  - httpie
  - lazygit
  - neovim
  - ripgrep
  - starship
  - tree-sitter

- cask:
  - 1password
  - docker
  - discord
  - firefox
  - font-symbols-only-nerd-font
  - insomnia
  - kitty
  - notion
  - rectangle
  - slack
  - tableplus
  - textmate
